# v23.2 Update — Framework 深拆 / 目录瘦身 / 高内聚低耦合重组计划

> 2026-04-25  
> 前置：`updates/v23-findings.md`、`updates/v23.1-update.md`  
> 定位：在 v23.1 已完成第一轮结构收敛之后，继续对 `framework/` 做 **第二轮深拆与目录瘦身**，把剩余的扁平文件、次级巨石、策略/配置/绘图/报告混放问题进一步拆开，让 framework 真正符合 **高内聚、低耦合、目录语义清晰** 的原则。

---

## 1. v23.2 一句话目标

> **把 v23.1 已经建立起来的 facade / services / core 骨架继续做实：继续拆分剩余巨石，继续下沉剩余混合职责，继续减少 framework 顶层扁平文件数量，让“代码放哪里”这件事在 framework 内部更稳定、更直观。**

v23.2 不做：

1. 不新增新的 research domain。
2. 不引入新的真实世界 provider 接入。
3. 不把重点转回 Gomoku domain。
4. 不推翻 v23.1 已经建立的兼容层策略。
5. 不为了目录好看而制造新的空抽象。

v23.2 要做：

1. 继续拆 `framework/facade/analyze_cli.py`。
2. 继续拆 `framework/facade/sweep_cli.py`、`branch_cli.py`、`promote_cli.py`。
3. 继续拆 `framework/services/research/selector_service.py`。
4. 继续拆 `framework/core/db/repo_recommendations.py`。
5. 把 policy / profile / plotting 相关文件迁到更明确的目录。
6. 减少 `framework/` 顶层非-wrapper 文件数量。
7. 在兼容旧 import / 旧命令的前提下，进一步明确文件落点。

---

## 2. 为什么 v23.2 是必要的

v23.1 已经证明第一轮结构收敛是有效的：

1. `core.db` 已 package 化。
2. 顶层 CLI 已退化为 wrapper。
3. Bayesian generic machinery 已回 framework。
4. 全量测试保持通过。

但 v23.1 的收口报告也同时明确指出：

1. `framework/facade/analyze_cli.py` 仍然非常大。
2. `framework/facade/branch_cli.py` / `sweep_cli.py` / `promote_cli.py` 仍然混合 CLI 与大量 planning / execution 逻辑。
3. `framework/services/research/selector_service.py` 仍然是新的次级巨石。
4. `framework/pareto_plot.py`、`stage_policy.py`、`branch_policy.py`、`selector_policy.py`、`search_space.py`、`objective_profile.py` 仍平铺在 framework 根目录。
5. `framework/core/db/repo_recommendations.py` 已开始变成 db package 内的新热点文件。

也就是说，v23.1 解决的是：

```text
先把边界拉出来
先把骨架立起来
先把最大的问题止血
```

而 v23.2 要解决的是：

```text
把骨架真正做实
把次级巨石继续打散
把目录语义继续收紧
```

所以 v23.2 不是 v23.1 的附录，而是：

> **在 v23.1 的基础上继续推进 framework 内部结构的“第二层拆分”。**

---

## 3. v23.1 后的实际现状判断

按照当前代码与测试状态，framework 内部已经进入下面这个阶段：

### 3.1 已基本成型的部分

1. `framework/core/db/`
2. `framework/facade/`
3. `framework/services/bayes/`
4. `framework/services/frontier/`
5. `framework/services/execution/`
6. `framework/services/research/`

这些目录说明：

> **v23.1 已经解决了“有没有落点”的问题。**

### 3.2 还没有完全做实的部分

当前最大的 framework Python 文件仍然包括：

| 文件 | 行数 | v23.2 判断 |
|---|---:|---|
| `framework/facade/analyze_cli.py` | 2611 | **P0，必须继续拆** |
| `framework/facade/branch_cli.py` | 667 | **P1，应该拆** |
| `framework/facade/sweep_cli.py` | 627 | **P1，应该拆** |
| `framework/services/research/selector_service.py` | 592 | **P1，应该拆** |
| `framework/core/db/repo_recommendations.py` | 491 | **P1，应该拆** |
| `framework/facade/promote_cli.py` | 448 | **P1，应该拆** |
| `framework/core/db/migrations.py` | 441 | 可接受，但需观察 |
| `framework/core/db/repo_runs.py` | 425 | 可接受，但需观察 |
| `framework/train.py` | 408 | 中等，需明确语义归属 |
| `framework/pareto_plot.py` | 407 | **P1，应迁移到 frontier plotting/export 层** |

### 3.3 根目录扁平文件仍然偏多

当前 `framework/` 根目录除了 wrapper 之外，还保留了大量“语义不够收敛”的文件：

1. `acquisition.py`
2. `acquisition_policy.py`
3. `stage_policy.py`
4. `branch_policy.py`
5. `selector_policy.py`
6. `search_space.py`
7. `objective_profile.py`
8. `pareto_plot.py`
9. `prepare_template.py`
10. `train.py`

这些文件的问题不是“本身错误”，而是：

1. 有的是 **policy**
2. 有的是 **profile / spec**
3. 有的是 **plotting / export**
4. 有的是 **template / runtime glue**

但它们现在仍被平铺在 root，导致：

> **“framework 根目录虽然已经比 v23 前清晰，但仍然没有真正瘦下来。”**

---

## 4. v23.2 的核心问题清单

| 编号 | 问题 | 当前状态 | v23.2 目标 |
|---|---|---|---|
| **B1** | analyze 仍过大 | `framework/facade/analyze_cli.py` 仍承担大量 reporting 命令 | 继续拆到 reporting / frontier / rendering service |
| **B2** | orchestration facade 仍偏重 | `sweep_cli.py` / `branch_cli.py` / `promote_cli.py` 仍混有大量 planner / executor 逻辑 | 继续拆到 execution / research service |
| **B3** | selector 成为次级巨石 | `selector_service.py` 既做 candidate generation，又做 scoring，又做 recommend ledger shaping | 拆为 candidate / scoring / recommendation service |
| **B4** | db package 内局部再巨石化 | `repo_recommendations.py` 吞掉 promotion / branch / surrogate / recommendation 四类职责 | 再拆 repo 子模块 |
| **B5** | 根目录仍有大量 policy / profile / plotting 文件 | policy / profile / plotting 语义未归位 | 创建 `policies/`、`profiles/`、frontier plotting/export 模块 |
| **B6** | wrapper 已有，但 root 仍不够瘦 | 用户仍会在 root 看到过多真实文件 | 让 root 尽量只保留 wrapper / shims / 少量真正的入口 |

---

## 5. v23.2 结构原则

v23.2 必须遵守下面 7 条原则：

1. **继续渐进迁移，不做大爆炸**
   - 仍然保留旧 import / 旧 CLI 路径
   - 用 shim / wrapper 继续过渡

2. **继续减少 root 中“真实逻辑文件”的数量**
   - root 保留 wrapper 没问题
   - 真实逻辑应继续向 `facade/`、`services/`、`policies/`、`profiles/` 收敛

3. **facade 只做 CLI / dispatch，不再承载重业务**
   - v23.2 的本质就是继续把 facade 中仍然过重的逻辑剥离出去

4. **service 只按职责聚合，不按“方便复制代码”聚合**
   - 例如：
     - frontier plotting / export 在一起
     - recommendation selection / scoring / batch write 分开

5. **policy / profile / plotting 必须有明确目录语义**
   - 不再继续平铺在 framework 根目录

6. **db package 允许二次拆分**
   - package 化不是终局
   - 某个 repo 模块重新长大后，应继续拆

7. **每一步都以 v23.1 的测试基线为硬约束**
   - 当前稳定基线：`187 passed`
   - v23.2 不能以“结构更漂亮”为代价破坏兼容

---

## 6. v23.2 目标目录形态

v23.2 的目标不是一次性完全终局，而是把目录推进到下面这个更清晰的阶段：

```text
framework/
  analyze.py
  sweep.py
  branch.py
  promote.py
  selector.py

  facade/
    analyze_cli.py
    sweep_cli.py
    branch_cli.py
    promote_cli.py

  services/
    frontier/
      pareto.py
      knee.py
      snapshots.py
      plotting.py
      exports.py
      labels.py

    execution/
      matrix.py
      workspace.py
      sweep_runner.py
      recommendation_execution.py

    research/
      selector_candidates.py
      selector_scoring.py
      selector_recommendations.py
      branch_service.py
      promotion_service.py

    reporting/
      run_reports.py
      campaign_reports.py
      recommendation_reports.py
      trajectory_reports.py
      renderers.py

    bayes/
      interfaces.py
      surrogate.py
      acquisition.py
      loop.py
      benchmark.py

  policies/
    stage_policy.py
    branch_policy.py
    selector_policy.py
    acquisition_policy.py

  profiles/
    search_space.py
    objective_profile.py

  core/
    db/
      connection.py
      schema_base.py
      migrations.py
      repo_runs.py
      repo_campaigns.py
      repo_branches.py
      repo_promotions.py
      repo_recommendations.py
      repo_surrogates.py
      repo_fx.py

  templates/
    prepare_template.py
    train_template.py
```

说明：

1. `policies/`
   - 专门收纳 policy loader / validator / helper

2. `profiles/`
   - 专门收纳 search-space / objective-profile 这类 domain-generic spec

3. `services/frontier/`
   - 除了 Pareto / snapshot，还应承接 plotting / export / labels

4. `services/research/`
   - 继续消化 selector / branch / promote 的 planner 层

5. `templates/`
   - 让 `prepare_template.py` / `train.py` 之类的“模板 / 胶水”文件不再混在 root

---

## 7. v23.2 Phase 总览

| Phase | 名称 | 核心动作 | 目标 |
|---|---|---|---|
| **Phase 0** | 目录语义冻结 | 冻结新的目录 taxonomy | 避免拆到一半目录名继续变 |
| **Phase 1** | Analyze 深拆 | 继续拆 `analyze_cli.py` | 把 reporting / rendering / frontier command 从 facade 中剥离 |
| **Phase 2** | Orchestration 深拆 | 拆 `sweep_cli.py` / `branch_cli.py` / `promote_cli.py` | 将 planner / executor 下沉到 services |
| **Phase 3** | Selector 深拆 | 拆 `selector_service.py` | candidate / scoring / recommendation 分层 |
| **Phase 4** | Policy / Profile 归位 | 建 `policies/`、`profiles/` | root 进一步瘦身 |
| **Phase 5** | Frontier plotting 重组 | 拆 `pareto_plot.py` | plotting / exports / labels 归 frontier |
| **Phase 6** | DB 二次拆分 | 拆 `repo_recommendations.py` | recommendation / promotion / branch / surrogate 各归其位 |
| **Phase 7** | 兼容清理与回归 | wrapper/import/CLI/test 全量回归 | v23.2 收口 |

---

## 8. Phase 0 — 目录语义冻结

### 8.1 目标

在继续拆代码之前，先把目录语义固定下来，避免一边拆、一边改目录名。

### 8.2 冻结的目录原则

1. `facade/`
   - 只放 CLI facade / dispatch

2. `services/`
   - 放有明确职责边界的业务 / 研究 / 计算逻辑

3. `core/`
   - 只放 infra / db / native / tui

4. `policies/`
   - 放 policy schema / loader / validator / helper

5. `profiles/`
   - 放 search-space / objective-profile 一类 profile / spec

6. `templates/`
   - 放模板 / default glue / shared train/prepare entry primitives

### 8.3 验收

1. 目录命名冻结
2. 后续 phase 不再反复更名

---

## 9. Phase 1 — Analyze 深拆

### 9.1 当前问题

`framework/facade/analyze_cli.py` 仍然是 framework 里最大的文件。

它现在仍同时承担：

1. run / compare / lineage / opponents 查询
2. campaign / stage / promotion / trajectory 报告
3. recommendation / acquisition / outcomes 报告
4. Pareto / knee / snapshot
5. report formatter / markdown / json rendering

### 9.2 v23.2 目标拆法

```text
framework/facade/analyze_cli.py
framework/services/reporting/run_reports.py
framework/services/reporting/campaign_reports.py
framework/services/reporting/recommendation_reports.py
framework/services/reporting/trajectory_reports.py
framework/services/reporting/renderers.py
framework/services/frontier/pareto.py
framework/services/frontier/knee.py
framework/services/frontier/snapshots.py
framework/services/frontier/plotting.py
framework/services/frontier/exports.py
```

### 9.3 具体动作

1. 把 `cmd_runs / cmd_best / cmd_frontier / cmd_compare / cmd_stability / cmd_stagnation` 拆到 `run_reports.py`
2. 把 `cmd_campaign_summary / cmd_stage_summary / cmd_promotion_log / cmd_promotion_chain` 拆到 `campaign_reports.py`
3. 把 `cmd_recommendation_log / cmd_recommendation_outcomes / cmd_acquisition_summary / cmd_recommend_next` 拆到 `recommendation_reports.py`
4. 把 `cmd_branch_tree / cmd_trajectory_report / cmd_compare_parent_child` 拆到 `trajectory_reports.py`
5. 把 markdown / json renderer 拆到 `renderers.py`
6. 把 Pareto plotting/export 再从 facade 中清走

### 9.4 验收

1. `analyze_cli.py` 显著降行数
2. `framework/analyze.py` 继续保持 wrapper
3. 旧命令参数行为不变

---

## 10. Phase 2 — Orchestration 深拆

### 10.1 目标对象

1. `framework/facade/sweep_cli.py`
2. `framework/facade/branch_cli.py`
3. `framework/facade/promote_cli.py`

### 10.2 现存问题

虽然 v23.1 已把它们从 root 搬到 facade，但这些文件本体仍然太重：

1. CLI parser
2. candidate / plan generation
3. command build
4. subprocess execution
5. recommendation execution
6. frontier delta / outcome write-back

仍然缠在一起。

### 10.3 v23.2 拆法

```text
framework/facade/sweep_cli.py
framework/services/execution/sweep_runner.py
framework/services/execution/recommendation_execution.py
framework/services/research/branch_service.py
framework/services/research/promotion_service.py
```

### 10.4 具体动作

1. `sweep_cli.py`
   - CLI 与 matrix/workspace 保留在 facade + execution
   - run orchestration / recommendation execute 下沉到 service

2. `branch_cli.py`
   - branch plan generation / parent resolution / child command build 下沉到 `branch_service.py`

3. `promote_cli.py`
   - promotion ranking / missing seed generation / execution 下沉到 `promotion_service.py`

### 10.5 与现有 pending fix 的关系

当前已有一批 pending issue 正好落在这些文件：

1. branch
2. promote
3. sweep
4. selector

v23.2 应把这些修复 **并入对应 phase 的深拆**，避免：

1. 先修一遍旧 facade
2. 再拆一遍新 service

造成双重触碰。

---

## 11. Phase 3 — Selector 深拆

### 11.1 当前问题

`framework/services/research/selector_service.py` 虽然从 root 迁走了，但本体仍然承担了三类职责：

1. candidate generation
2. candidate scoring / dominance / ranking
3. recommendation batch shaping / id generation

### 11.2 v23.2 目标拆法

```text
framework/services/research/selector_candidates.py
framework/services/research/selector_scoring.py
framework/services/research/selector_recommendations.py
framework/services/research/selector_service.py   # thin coordinator
```

### 11.3 具体动作

1. generation 相关移到 `selector_candidates.py`
2. `_score_candidate` / `_is_dominated` / utility helpers 移到 `selector_scoring.py`
3. `recommend_for_campaign` / `build_recommendation_id` 等 recommendation assembly 移到 `selector_recommendations.py`
4. `selector_service.py` 最终只保留 orchestration

### 11.4 验收

1. 保持旧 `framework/selector.py` shim 可用
2. 旧测试中的 internal symbol import 仍可兼容

---

## 12. Phase 4 — Policy / Profile 归位

### 12.1 当前问题

这些文件现在语义清晰，但位置不清晰：

1. `stage_policy.py`
2. `branch_policy.py`
3. `selector_policy.py`
4. `acquisition_policy.py`
5. `search_space.py`
6. `objective_profile.py`

### 12.2 v23.2 目标

建立：

```text
framework/policies/
framework/profiles/
```

### 12.3 迁移建议

#### policies

1. `stage_policy.py`
2. `branch_policy.py`
3. `selector_policy.py`
4. `acquisition_policy.py`

#### profiles

1. `search_space.py`
2. `objective_profile.py`

### 12.4 兼容策略

根目录保留轻量 shim：

1. `framework/stage_policy.py`
2. `framework/branch_policy.py`
3. `framework/selector_policy.py`
4. `framework/acquisition_policy.py`
5. `framework/search_space.py`
6. `framework/objective_profile.py`

都继续可 import，但真实实现迁到新目录。

---

## 13. Phase 5 — Frontier plotting / export 重组

### 13.1 当前问题

`framework/pareto_plot.py` 已不只是“画图”，而是混合了：

1. plotting
2. annotation
3. table export
4. smoothed frontier overlay
5. front-only / knee plot artifact 输出

### 13.2 v23.2 目标拆法

```text
framework/services/frontier/plotting.py
framework/services/frontier/exports.py
framework/services/frontier/labels.py
framework/pareto_plot.py   # compatibility shim
```

### 13.3 验收

1. `plot_pareto_artifacts()` 旧入口不回退
2. plotting / export / label formatting 不再挤在同一个文件里

---

## 14. Phase 6 — DB 二次拆分

### 14.1 当前问题

`framework/core/db/repo_recommendations.py` 已经吸收了：

1. promotion decisions
2. run branches
3. recommendation batches / items
4. surrogate snapshots
5. recommendation outcomes

这已经接近新的局部巨石。

### 14.2 v23.2 目标拆法

```text
framework/core/db/repo_promotions.py
framework/core/db/repo_branches.py
framework/core/db/repo_recommendations.py
framework/core/db/repo_surrogates.py
```

### 14.3 原则

1. branch / promotion / surrogate / recommendation 各自独立
2. `framework/core/db/__init__.py` 继续统一 re-export
3. 旧 `from core.db import ...` 不应中断

---

## 15. Phase 7 — 兼容清理与测试计划

v23.2 是结构重组，不是纯文档更新，因此必须以 v23.1 的测试基线为硬约束。

### 15.1 必跑回归

1. `uv run python framework/analyze.py --help`
2. `uv run python framework/sweep.py --help`
3. `uv run python framework/branch.py --help`
4. `uv run python framework/promote.py --help`
5. `uv run python domains/fx_spot/bayesian_refine.py --help`

### 15.2 必跑全量测试

```text
uv run python -m pytest tests/ -q
```

v23.1 的稳定基线是：

```text
187 passed
```

v23.2 需要继续守住这个基线。

### 15.3 必跑重点场景

1. `fx_spot` workspace smoke
2. `fx_spot` Bayesian replay smoke
3. analyze Pareto / plot
4. branch / promotion / selector / recommendation 相关测试

### 15.4 文档回填要求

完成后，应在 `updates/v23.2-update.md` 底部追加：

1. 目录对照
2. 文件迁移清单
3. compatibility shim 清单
4. 测试结果
5. 本轮继续未拆完的剩余债务

---

## 16. v23.2 推荐实施顺序

正确顺序应是：

1. **冻结目录语义**
2. **先拆 analyze**
3. **再拆 orchestration facade**
4. **再拆 selector**
5. **再迁 policy / profile**
6. **再拆 pareto_plot**
7. **最后做 repo_recommendations 二次拆分**

理由：

1. `analyze_cli.py` 仍然是现在最大的 framework 文件，必须先处理
2. orchestration / selector 正好和现有 pending issue 高度重合，应该一起处理
3. policy / profile 迁移属于“root 瘦身”，应在行为逻辑稳定后做
4. DB 二次拆分要放后面，避免同时改动太多读写路径

---

## 17. v23.2 的最终目标状态

v23.1 让我们从：

```text
没有结构落点
最大巨石失控
Bayesian 边界错位
```

收敛到：

```text
有了 facade/services/core
最大的两个问题已经止血
兼容层已建立
```

v23.2 要继续把系统推进到：

```text
剩余次级巨石被继续打散
root 明显变瘦
policy/profile/plotting 各归其位
facade 更薄，service 更聚焦
```

更直接地说：

> **v23.1 是“把结构搭起来”，v23.2 则是“把结构做细、做实、做瘦”。**

---

## 18. v23.2 完成后必须回填的内容

完成后，必须在本文底部追加独立收口章节，至少包括：

1. v23.1 → v23.2 的目录变化对照
2. 迁移到 `policies/`、`profiles/`、`services/reporting/`、`services/frontier/` 的文件清单
3. `analyze_cli.py` / `sweep_cli.py` / `branch_cli.py` / `promote_cli.py` / `selector_service.py` 的拆分前后行数对照
4. `repo_recommendations.py` 二次拆分结果
5. compatibility shim 清单
6. CLI 回归结果
7. 全量测试结果
8. 本轮仍未消化的剩余结构债务与 v23.3 建议
